pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the web app...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to local Apache server...'
            }
        }
    }
}
